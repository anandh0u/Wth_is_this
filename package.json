{
  "name": "wth-is-this",
  "version": "0.1.0",
  "private": true,
  "productName": "WTH Is This",
  "description": "An anti-productivity desktop pet with browser privileges.",
  "main": "desktop/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check desktop/main.js && node --check desktop/preload.js && node --check desktop/renderer.js && node --check desktop/audio.js && node --check desktop/ai/lui-engine.js && node --check desktop/panels/panels.js && node --check extension/background.js && node --check extension/content.js",
    "test": "node --test"
  },
  "devDependencies": {
    "electron": "^44.3.0"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
